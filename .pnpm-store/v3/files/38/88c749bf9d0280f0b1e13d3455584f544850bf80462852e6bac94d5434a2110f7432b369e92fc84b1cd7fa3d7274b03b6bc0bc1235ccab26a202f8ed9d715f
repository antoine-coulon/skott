/**
 * Returns a default `of` composition.
 *
 * @since 1.0.0
 */
export const ofComposition = (F, G) => a => F.of(G.of(a));
/**
 * @since 1.0.0
 */
export const unit = F => F.of(undefined);
/**
 * @category do notation
 * @since 1.0.0
 */
export const Do = F => F.of({});
//# sourceMappingURL=Of.mjs.map